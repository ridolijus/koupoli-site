import { Link } from "wouter";
import SiteLayout from "@/components/SiteLayout";
import { blogPost, site } from "@/lib/siteData";

export default function BlogPost() {
  return <SiteLayout><article><header className="article-hero webflow-glow"><div className="narrow-container"><p className="blog-date">{blogPost.date}</p><h1>{blogPost.title}</h1><p>{blogPost.excerpt}</p></div></header><section className="article-section"><div className="narrow-container"><img className="article-image" src={site.articleImage} alt="Artificial intelligence productivity illustration" /><div className="article-rich-text"><p>Welcome to the future, where artificial intelligence promises to make us all faster, smarter, and, if the marketing is to be believed, <strong>better looking</strong>. But before you hand over your next big project to your favourite chatbot, let&apos;s take a stroll through the real impact of AI on productivity, creativity, and the human ability to get stuck in an infinite loop of “where do I even start?”</p>{blogPost.sections.map((section) => <section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</section>)}<section><h2>The Guide to Surviving the AI Revolution</h2><p>So, what&apos;s the secret to thriving in an AI-powered world? Here&apos;s a survival guide:</p><ul>{blogPost.guide.map((item) => <li key={item}>{item}</li>)}</ul></section><section><h2>Conclusion (classic AI bot end of article): The Infinite AI</h2>{blogPost.conclusion.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</section></div><section className="more-blogs"><h2>More Blogs</h2><p>No items found.</p><Link className="inline-link" href="/blog">Back to blogs</Link></section></div></section></article></SiteLayout>;
}
